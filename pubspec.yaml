name: game_tracker
description: "Game Tracking app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  isar_community: ^3.3.0
  isar_community_flutter_libs: ^3.3.0
  flutter_bloc: ^9.1.1
  bloc_test: ^10.0.0
  google_sign_in: ^7.2.0
  envied: ^1.3.3
  # Firebase
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  # Mocking Data
  mocktail: ^1.0.4
  # GetIt
  get_it: ^9.2.0

dev_dependencies:
  isar_community_generator: ^3.3.0
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  envied_generator: ^1.3.1
  build_runner: ^2.7.1

flutter:
  uses-material-design: true